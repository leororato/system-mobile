import { StyleSheet } from "react-native";

export const loginStyle = StyleSheet.create({
    inputLogin: {
        width: "80%",
        padding: 10,
        marginBottom: 10,
        borderWidth: 1,
        borderColor: "#ccc"
    }
})